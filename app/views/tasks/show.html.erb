<div class="container">
  <div class="box-tasks">
    <h1><%= @task.title %></h1>
    <div class="box-details">
      <p><%= @task.details %></p>
    </div>
    <p>(<%= @task.completed %>)
      <% if @task.completed %>
        <%= "This task is compelted" %>
      <% else %>
        <%= "This task is not completed yet" %>
      <% end %>
    </p>
    <%= link_to 'Back to list', tasks_path %>
  </div>
</div>
